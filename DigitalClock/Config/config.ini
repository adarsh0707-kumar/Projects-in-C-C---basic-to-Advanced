# =========================================================
# Digital Clock System - Configuration
# =========================================================
# Lines beginning with '#' or ';' are comments.
# Keys are matched case-insensitively.
# Invalid values are replaced by the defaults shown here.
#
# A key the application does not recognise is reported: as a warning in
# the log, and as an "unrecognised key(s)" row in the status bar. A
# misspelt key has no effect, and without the warning that is
# indistinguishable from a setting left out on purpose.
#
# Press C while the clock is running to re-read this file. Press T to
# change theme without editing anything.
# =========================================================

# ---------------------------------------------------------
# Display
# ---------------------------------------------------------

# Clock format: 12 or 24.
TimeFormat=24

# Date format: LONG, DD-MM-YYYY, MM-DD-YYYY or YYYY-MM-DD.
DateFormat=LONG

# Colour theme: Dark, Light, Blue, Green or HighContrast.
#
# Theme files live in Resources/themes and name a colour per element,
# optionally followed by text styles:
#
#   TIME=BrightGreen Bold
#   FOOTER=BrightBlack Dim
#
# Styles: Bold, Dim, Italic, Underline, Reverse, Normal. Not every
# terminal renders Italic.
Theme=Dark

# Redraw interval in milliseconds. Accepted range: 50 - 60000.
RefreshInterval=1000

# ---------------------------------------------------------
# Resources
# ---------------------------------------------------------

# Banner artwork shown at the top of the screen.
Banner=Resources/banner.txt

# ---------------------------------------------------------
# Alarms
# ---------------------------------------------------------

# Master switch for the alarm module.
Alarms=Enabled

# File holding the alarm definitions.
AlarmFile=Config/alarms.ini

# Snooze delay in minutes. Accepted range: 1 - 240.
SnoozeMinutes=5

# Emit the terminal bell while an alarm is ringing.
AlarmBell=true

# ---------------------------------------------------------
# World Clock
# ---------------------------------------------------------

# Additional time zones, comma separated, shown in World mode (press M).
# Each entry is a specification with an optional label after a pipe:
#
#   UTC                     fixed, no daylight saving
#   +05:30 | Home           fixed offset
#   Asia/Kolkata | Home     named zone, follows daylight saving
#
# Named zones need the platform timezone database. That is present on
# Linux and macOS; on Windows use fixed offsets instead. Leave empty to
# hide the World mode entirely.
TimeZones=

# ---------------------------------------------------------
# Stopwatch and Timer
# ---------------------------------------------------------

# Starting duration for the countdown timer.
# Accepts MM:SS, H:MM:SS, or a bare number of seconds. Max 24 hours.
TimerDuration=05:00

# ---------------------------------------------------------
# Logging
# ---------------------------------------------------------

# Master switch: Enabled or Disabled.
Logging=Enabled

# Log file path. Missing directories are created automatically.
LogFile=Logs/application.log

# Lowest severity recorded: DEBUG, INFO, WARNING or ERROR.
LogLevel=INFO

# Mirror log entries to the terminal. Leave false; console output
# would interfere with the clock rendering.
ConsoleLog=false
